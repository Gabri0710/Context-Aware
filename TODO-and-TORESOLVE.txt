RESOLVE:
- PROBLEMA. SE FACCIO IL LOGOUT E RIENTRO CON UN ALTRO UTENTE, LA POSIZIONE VIENE AGGIORNATA A ENTRAMBI GLI UTENTI.
- PROBLEMA. QUANDO AGGIUNGO UN NUOVO UTENTE, MI INSERISCE ANCHE UN RECORD CON USERNAME VUOTO.
- i 2 problemi sopra sono perché la roba nella mainActivity viene creata nell'OnStart (2 perche quando apro l'app l'username non è assegnato, è uguale a "" e quindi inserisce l'utente vuoto) (1 perche l'ultimo valore memorizzato nella variabile username è assegnata all'utente con il quale poi si fa logout)
- FIX METODO ELBOW BACKEND (NON FUNZIONA BENE)

TODO:
- refactor app mobile e fix avvio delle variabili nell'OnCreate
- Metodo Elbow può essere eseguito solo una volta (scazza con matplotlib eseguito su thread non-principale (non ha senso ma ok))
- colori utenti del cluster sovrapposti dal colore dei geofence perché i geofence sono in continuo richiamo
- inserire grafica in frontend